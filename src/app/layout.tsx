import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Hatton Garden | London's Historic Diamond District",
  description: "Experience the heart of luxury jewellery in London's historic Hatton Garden. Discover world-class jewellers, artisan craftsmanship, and vibrant local life.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.className} antialiased`}>
        {children}
      </body>
    </html>
  );
}
